{
  "name": "ticketharbor",
  "version": "1.0.0",
  "private": true,
  "description": "Root package for TicketHarbor multi-category ticket booking platform",
  "scripts": {
    "build": "npm install --prefix server",
    "start": "npm start --prefix server",
    "seed": "npm run seed --prefix server"
  }
}
